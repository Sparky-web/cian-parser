{
  "routes": [
    {
      "method": "GET",
      "path": "/proxies",
      "handler": "proxies.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/proxies/count",
      "handler": "proxies.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/proxies/:id",
      "handler": "proxies.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/proxies",
      "handler": "proxies.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/proxies/:id",
      "handler": "proxies.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/proxies/:id",
      "handler": "proxies.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
